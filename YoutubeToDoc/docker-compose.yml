version: '3.8'

services:
  youtubetodoc:
    build: .
    container_name: youtubetodoc_app
    ports:
      - "8083:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
