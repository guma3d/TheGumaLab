# GumaPhoto 프로젝트 진행 상황 (Progress Log)

이 문서는 GumaPhoto 서비스의 개발 진행 상황과 중요 테스트/기술 결정 내역을 기록하는 공간입니다.

## 📌 [1단계] 인프라 및 기본 구조 셋업 세팅 (완료)
*   **Docker 및 FastAPI 구축:** 포트 매핑 및 Reverse Proxy 설정 완료.
*   **파일 시스템(Clean Architecture) 재정렬:** 어지럽게 분산되어 있던 50여 개의 진단 스크립트를 용도에 맞게 분리 및 영구 폐기.

## 📌 [2단계] 맞춤형 안면 인식 패러다임 점검 (완료)
*   **모델 업그레이드:** 오픈소스 최고 권위 `InsightFace(buffalo_l)` 및 PyTorch 시간차 경량화 표정 엔진 `HSEmotion`으로 전격 세대교체 달성.

## 📌 [3단계] 갤러리 물리적 정리 및 파이프라인 고도화 (완료)
*   **충돌 방어 (Collision):** 베이스 네임 일치 시 뒷번호 밀어넣기(Hole-Filling) 방어 세팅 완료.
*   **글로벌 한글 강제 매핑 (Geocoding-Ko):** OpenStreetMap 역추적 API에 `language='ko'`를 주입.
*   **쓰레기 필터 (Junk/Hash):** 스크린샷 찌꺼기와 완전 중복된 사진들 즉각 영구 삭제 로직.

## 📌 [4단계] 멀티모달 벡터 검색 및 캡셔닝 아키텍처 설계 (완료)
*   **다국어 SigLIP 768D + Florence-2 캡셔닝 융합:** 
    *   Florence-2 Bfloat16 캐주얼 정밀도로 구동 속도 극대화. 
    *   안정적인 **배치 10장** 처리 한계 설정 및 무한 루프 텍스트 환각 방어장치 (Repetition Penalty).
*   **태그 사전 자동 갱신 (Self-Healing Vocab):** 인덱서 돌입 후 `/app/data/available_tags.json` 마스터 사전에 자가 업데이트 실시.

## 📌 [5단계] AI 검색 엔진(LLM 파서)의 무결성 달성 (완료)
*   **환각(알래스카 할루시네이션) 원천 방어:** 빈 값 패스 로직을 발동시켜 아예 엉뚱한 장소로 창박해 내뱉는 Gemini의 지어내기(유추)를 철벽 통제.

## 📌 [6단계] 100% 완전 파기 묘비 (Hard Delete & Tombstone) (완료)
*   원본 사진, DB 페이로드, 그리고 파생 썸네일(`_heic.webp`) 바이너리까지 100% 물리 동반 파괴 적용. 
*   또한 이미 지워진 구 파일들이 유령처럼 부활하여 AI 엔진을 맴도는 것을 막기 위해 `status='DELETED'` 영구 사망 마크 각인 연계 완비.

## 📌 [7단계] 100% 무결점 피드백 자율 진화 시스템 v2.0 (완료)
*   **단일 사진 노출 포커싱:** 피드백 모달창에서 미분류 1장만을 직관적으로 노출시켜 편향 없는 1/n 무작위 수선 분배 구조 도출.
*   **ExifTool 주입 체계:** HEIC, MP4의 메타데이터를 100% 파싱 및 물리 하드코딩 업데이트 후 시스템 백지화(Re-build) 이주 처리 완료.

## 📌 [8단계] Premium SaaS UI/UX 전면 개편 (완료)
*   **초스무스 비동기 SPA 전환:** iOS 사파리 감성의 네이티브 액션 클립 및 글라스모피즘 탭 라우팅(`frontend/`) 개명. 랜더링 홀 부재 방어를 위한 10칸 무조건 슬라이더 보장 렌더 구성.

## 📌 [9단계] Enterprise 클린 모듈(Domain-Driven API) 아키텍처 (완료)
*   `vector_indexer.py`, `organizer_pipeline.py` 처럼 500줄이 넘어가던 모놀리식 뚱뚱한 코드 전체를 썰어 `api/routers/` (서빙 API) 와 `api/services/` (비즈니스 AI 공장) 내부의 파츠 6개로 쪼개고 완전히 분리 매핑 완료.
*   **Redis + Celery 로드 밸런싱 통제:** `worker_max_tasks_per_child=1` GPU 캐시 스왑을 강제 적용해 평생 무한히 사진을 구워도 PyTorch OOM 버그가 생기지 않는 기적 달성.

## 📌 [10단계] 전역 데이터베이스 단일 객체화 (SQLAlchemy ORM 도입) (완료)
*   **Raw SQLite 장부 전면 폐기:** 라우터와 인덱서 모듈 사방에 "INSERT OR REPLACE" 등 지저분하게 흩날리며 락다운 에러(DB is Locked)를 유발하던 낡은 하드코딩 쿼리를 영구 소각했습니다.
*   **단일의 객체 진실 공급원 (Single Source of Truth) 마련:** SQLite의 복잡한 테이블(`processed_files`, `vectorized_files` 등)을 뭉쳐내어, `core.models.py` 안에 **`Photo`** 라는 완벽한 마스터 객체 하나로 통합 설계했습니다.
    *   **정규 파라미터 매칭:** 사진 하나가 정리(Organize)될 때, 즉시 `width`, `height`, `file_size_bytes` 해상도 규격을 측정해 DB 객체에 미리 박아넣어, 추후 프론트엔드가 모바일 스크롤 잔상 현상(CLS) 없이 찰떡같이 렌더링되게 엮었습니다.
    *   **통합 Status 추적기:** 파편화 테이블 삭제 기록이 아닌, 단지 **`status='UPLOADED' -> 'ORGANIZED' -> 'VECTORIZED' -> 'DELETED'`** 속성 하나의 변경에 따라 시스템의 배턴 터치 흐름이 부드럽게 이어지는 극강의 OOP적 유지보수를 이룩했습니다.

## 📌 [11단계] 도메인 로직(Utils) 순수 분리 및 UI 버그 헌팅 (완료 🎯)
*   **`PhotoPurger` (데이터 말소 전담팀) 신설:** `delete.py` 및 피드백 모듈 내부에서 날것으로 굴러다니던 위험한 쓰레기 하드 딜리트(Qdrant, XMP, SQLite) 코드를 하나의 순수 유틸리티(Utility)로 100% 분리. `keep_original` 플래그 하나로 완전 파쇄 vs 피드백 보존을 마법처럼 통제 처리.
*   **`MetadataEditor` (Exif 전담팀) 신설:** 피드백이 내려찍히면 무지성으로 EXIF와 지오코딩만 수행하는 독립 모듈을 만들어 기존 거대 라우터를 수십 줄 다이어트 성공.
*   **프론트엔드 Scan 블로킹 오류 완벽 타파:** Qdrant 벡터 검색 시 자기 자신을 제외하거나 유사도 임계점 이하라서 화면이 먹통이 되던 버그를 고치기 위해, 타겟 백엔드에서 원본 사진 1장을 Index 0 에 무조건 강제 주입하도록 덮어씌움 처리.
*   **유령 깡통 폴더 영구 청소망 구축:** 피드백/삭제 메커니즘으로 인해 파일이 다 스왑되어 생기는 `2025-10` 같은 비어있는 고스트 폴더들을 오거나이저 동작 직후 `os.walk(topdown=False)` 로 가장 아랫바닥부터 훑으며 무자비하게 OS 단에서 삭제하도록 방어 로직 전개.
*   **풀-듀플렉스(Full-Duplex) 로깅 디스플레이 탑재:** Windows 사용자 친화적인 `View_Live_Logs.bat` 를 새롭게 개조하여, `gumaphoto_app` (FastAPI 통신망) 과 `celery_worker` (AI 기계실) 이 내뱉는 로그를 동일 프레임(터미널)에 시각적으로 태그를 붙여 통합 생중계 가능하도록 배포 완료.

## 📌 [12단계] 디테일 UX 고도화 및 네이티브 모바일 최적화 (완료 🎯)
*   **Web Share API Level 2 (물리적 파일 공유):** iOS/Android에서 사진 공유 시 단순 URL 텍스트가 전송되어 카카오톡 등에서 접근 권한 에러가 나던 현상을 타파. 클라이언트에서 Blob 데이터를 즉각 Fetch하여 `File` 객체로 물리적 패키징 후 네이티브 공유 시트에 던지는 방식으로 카카오톡 원본 직배송 완벽 호환 구현.
*   **PWA 및 네이티브 터치 블록(iOS 최적화):** 사파리의 고질적인 꾹 누르기 드래그(Lift) 효과나 하이라이트 발생 시 모서리 곡률(`border-radius`)이 날아가는 버그를 `-webkit-user-drag: none;` 단에서 완전 봉인.
*   **Pinch-to-Zoom 뷰어 탑재:** `Panzoom.js` 프레임워크를 이식하여 두 손가락 부드러운 줌인/아웃 구현 + 화면 단일 탭 시 거추장스러운 UI들만 스르르 사라지는(Fade 오파시티 토글) 영화 같은 몰입 뷰(Focus Mode) 완성.
*   **자동 고스트 롤백(Ghost Rollback) 방어 체계:** 사용자가 피드백 UI에서 동명이인이나 엉뚱한 인물로 잘못 태깅하고 재교정을 시도할 때, 구 폴더에 떨어져 있던 가짜 얼굴 크롭(`{point_id}.jpg`)을 `glob`로 글로벌 추적 후 원천 소각하는 롤백 메커니즘을 융합 완료.
*   **Windows 배치 스크립트 인코딩 가드:** 한글을 포함해 `chcp 65001` 선언 시 Windows `cmd.exe` 버그로 인해 `docker` 명령어나 변수가 바이트 밀림으로 잘리던 고질적 버그(`aphoto_app`)를 100% 영문 ASCII 변환을 통해 영구 해결.

## 📌 [13단계] 단일 진실 공급원(Qdrant) 아키텍처 완성과 독립 AI 모듈화 (완료 🎯)
*   **완벽한 Qdrant 단일화 (SQLite 완전 소각):** 피드백, 인덱서, 검색 등 모든 도메인에서 `SessionLocal`, `Photo` ORM 접근을 полностью 제거하여 파편화된 DB 동기화 오류를 원천 차단. 메타데이터와 벡터를 모두 품은 Qdrant 단일 생태계 확립.
*   **InsightFace 완전 독립 모듈화 (`InsightFaceModule`):** `vector_indexer`에 종속되어 있던 안면 인식 엔진을 `api/services/insightface_service.py`로 분리. 장소/날짜 피드백 시 불필요한 안면 분석을 생략(`skip_face=True`)할 수 있게 되어 Qdrant 덮어쓰기 파이프라인 속도가 비약적으로 상승.
*   **피드백 덮어쓰기(Overwrite) 메커니즘 전환:** 
    *   장소/날짜 피드백: 기존의 `PhotoPurger` 파쇄 후 `uploads_raw` 재업로드 큐를 타던 비효율적 롤백 방식에서 탈피하여, `MetadataEditor`로 EXIF를 영구 수정 후 즉시 Qdrant에 벡터를 덮어쓰도록(Overwrite) 진화.
## 📌 [14단계] 피드백 데이터 무결점 감시 및 시각화 고도화 (완료 🎯)
*   **물리적 Exif 완전 통치 (Location/Date):** 기존의 임시방편이었던 폴더 이름 기반 장소/날짜 파싱 트릭을 영구 소각하고, 온전히 `ExifTool`이 주입한 실제 파일 메타데이터만을 진실의 원천(Source of Truth)으로 삼아 Qdrant 및 백엔드를 동기화하도록 아키텍처 재편.
*   **Celery Scope-Shadowing 버그 픽스:** 파이썬 `import json` 로컬 변수 섀도잉으로 인해 일괄 처리가 1장으로 끊어지던 치명적 배치(Batch) 오류를 완벽히 타파, 대규모 사진 N장에 대한 즉각적 멀티 피드백 적용 성공.
*   **Audit Logger (블랙박스) 체제 도입:** 메타데이터 변조가 발생할 때마다 변경 전(Before)과 변경 후(After)의 실제 생태를 `.json` 로그 파일로 낱낱이 남기는 회계 감시망(`audit_trace.json`) 작동 및 Qdrant Retrieve 연계 입증 지원.
*   **프론트엔드 Audit UI 대규모 모달(Modal) 개사:** System 탭 구석에 흉하게 방치되던 텍스트 로그 컨테이너를 잘라내어, 프리미엄 카드 버튼 + 글라스모피즘 팝업 모달 + 최대 50건 스크롤 뷰 형식으로 완벽한 통일성 부여 및 브라우저 캐시 무력화(`v=4` 멱등성 갱신) 달성.
*   **피드백 Scan 레이어 썸네일 초고속화:** 유사 사진 체크박스(Check) 뷰에서 무지성으로 다운로드 되던 20MB 원본 코어(`.jpg`) 대신, 초경량 썸네일 파일(`.webp`) 경로를 우선 탐색하는 Fallback 알고리즘 렌더기를 장착하여 렌더링 스피드 한계 돌파.

## 📌 [15단계] 시스템 진실의 방(Source of Truth) 구조 완전 확립 및 내구성 집중 강화 (완료 🎯 - 오늘 진행)
*   **Vector Indexer 엔진 풀-업그레이드:** `vector_indexer.py` 본체에 OSM 글로벌 지능형 역 지오코딩(Reverse Geocoding) 모바일 캐시 시스템을 탑재하여 Kakao API의 국내 한계를 돌파. 또한 GPU 벡터 연산과 동시에 프론트엔드 최적화용 WebP 썸네일을 배치(batch) 내에서 병렬로 구워내는 아키텍처로 완전 결합.
*   **NLP 장소 파서(Parser) 토큰화 혁신:** 검색 엔진(`search.py`)에서 과거 폴더명에 의존하던 하이픈(`-`) 스플릿 의존성을 완전히 타파. OSM이 뱉어내는 띄어쓰기(` `) 베이스의 다국어 주소 정규표현식(`re.split`)으로 정밀 토큰화(`~구`, `~동` 접미사 제거)를 완벽히 교정하여 글로벌 자연어 검색 안정화.
*   **InsightFace Age-Progressive 매핑 대응:** 나이에 따라 `준우_1`, `준우_2` 등 세분화된 얼굴 도감으로 학습시키더라도, Qdrant 페이로드와 Frontend 검색 상에서는 무조건 깨끗한 본명(`준우`) 통일되도록 `split('_')[0]` 강제 정규화 파이프라인 검증 및 탑재.
*   **FastAPI 콜드-스타트(Cold-Start) 수문장 방어:** 딥러닝 워커가 VRAM에 모델을 올리고 Qdrant Collection을 생성하기도 전에 프론트엔드가 접속하여 404 Not Found로 백엔드가 치명적 크래시 되는 현상을 타파. `collection_exists` 예외(Exception)를 우아하게 잡아내어 유저에게 '데이터베이스 초기화 중'이라는 친절한 Payload를 반환하도록 생존성 패치.
*   **파이썬 로컬 섀도잉(UnboundLocalError) 버그 발본색원:** `search.py` 하단부 깊숙한 곳의 `try... import os` 하나로 인해, 상단부 전체의 `os.path` 네임스페이스가 오염되던 파이썬 호이스팅 버그를 추적/격리/삭제하여 치명적 백엔드 마비 요인 제거.

## 📌 [16단계] 인프라 완전 자립 최적화 및 편의 유틸리티 팩 확장 (완료 🎯 - 오늘 진행)
*   **완전 무결점 ExifTool 내장화 (Native Engine):** 기존 OS 셸 환경이나 환경변수에 의존하던 외부 `exiftool`을 Perl 기반 Standalone 바이너리 단위로 직접 Git 레포지토리 폴더(`tool/exiftool_engine`) 내부에 박아넣어, 어느 OS나 Docker 컨테이너에서든 환경 셋업 없이 100% 작동을 보장하는 네이티브 통합 구동 달성.
*   **사전 카카오톡 장소 태깅망(Preprocess Tool) 구축:** 사진이 Qdrant에 올라가기 전, Python의 대화형 터미널에서 `Scripts/preprocess_tool.py`를 가동해 사용자가 직접 카카오맵 기반으로 특정 사진의 장소 좌표를 선태그(pre-injection)할 수 있는 무적의 편의성 유틸리티 완성.
*   **공장 초기화 엔진 (Factory Reset DB):** 데이터 정합성이 깨진 경우 셸에서 폴더들을 수동으로 지우는 노동을 방지하기 위해, Qdrant Collection 소각부터 SQLite 비우기, 파생 폴더 리셋까지 한 방에 해결하는 `Scripts/factory_reset_db.py` 배치 파일 연동.
## 📌 [17단계] 장소 파싱 정밀화 및 AI 검색 아키텍처 완전 개편 (완료 🎯 - 오늘 진행)
*   **XMP 메타데이터 완전 폐기 및 GPS-Only 단일화:** 과거 Adobe 표준인 XMP 규격을 파싱하다 발생한 외계어 텍스트(`??ǳ`) 오염을 뿌리뽑고자, XMP 파싱 및 주입 로직을 `preprocess_tool.py`, `orchestrator.py` 등 모든 곳에서 영구 삭제. 오직 원본 GPS 좌표(Latitude/Longitude)만을 Source of Truth로 삼아 OSM(Nominatim) 역번역으로만 장소를 구축하는 청정 DB로 마이그레이션 성공.
*   **Vector Indexer 초경량 모듈 스위치(`--update-location`) 탑재:** 기존의 거대한 AI 인덱서를 5개의 토글 스위치(`run_vision_ai`, `skip_face`, `run_metadata_geo` 등)로 분할. 무거운 SigLIP, Florence-2를 VRAM에 올리지 않고 CPU만 사용하여 수만 장의 DB 페이로드를 초당 수백 장씩 광속으로 덮어쓰기(`set_payload`)하는 혁신적 DB 패치 파이프라인 도입.
*   **Qdrant TEXT 인덱스 `MatchAny` 버그 픽스:** Qdrant 내부 수학 엔진이 문자열 배열 매칭 시 TEXT 자료형에서 `MatchAny` 연산을 무시하며 0건을 반환하던 치명적 버그를 색출. 이를 `Filter(should=[FieldCondition(MatchText)])` 방식의 OR 중첩 쿼리문으로 완벽히 치환하여 검색 누락 해결.
*   **Gemini 3.1 Flash 지능형 엔티티 추출(Entity Resolution) 도입:** "하와이 사진 보여줘" 같은 자연어에서 정규표현식으로 행정구역(특별시, 도)을 떼어내며 억지로 비교하던 `difflib` 문자열 슬라이싱 코드를 전면 폐기. 오로지 `available_tags.json` 리스트를 통째로 Gemini에 넘겨 "사용자 의도에 맞는 지명을 리스트에서 골라만 줘" 라고 책임을 위임하는 차세대 LLM 매핑 방식을 도입하여 `["미국 Hawaii"]` 와 같은 영문/국문 크로스 매칭을 100% 무결점으로 달성.
## 📌 [18단계] 3D 지구 렌더링 검토 및 실시간 모니터링 시스템 구축 (완료 🎯 - 오늘 진행)
*   **비동기 Qdrant 호출 블로킹 방어 엔진:** GeoJSON 핀 생성용 라우터(`search.py`) 내부에서 순차적 I/O 동기(Synchronous) 작업인 Qdrant 쿼리가 FastAPI의 `async` 비동기 메인 이벤트 루프를 장악하여 UI 무한 로딩(블로킹)을 유발하던 버그를 해결. 이를 안전한 스레드 풀 할당(`def` 선언)으로 전환하여 프론트엔드 반응성 즉시 복구.
*   **글로벌 라이브 프로세싱 시스템 모달 탑재:** 백그라운드에서 동작 중인 AI 인덱싱(현재 `--update-location` 약 2.6만 장 배치 연산) 과정의 stdout 텍스트 로그(`/app/data/indexer_geo_log.txt`)를 인터셉트하여, 프론트엔드에서 주기적으로 읽어들이는 **'Live Progress Monitor' (<i class="fa-solid fa-list-check"></i>)** 전용 글라스모피즘 버튼과 듀플렉스 팝업 창을 `index.html`에 성공적으로 이식 완료. (현재 모바일 대응 버전 `v=15` 적용)
*   **Three.js 방식의 CesiumJS 광환(Glow) 이식 검토 작업:** 
    *   초기: 단순 확대 구체(`scale 1.05`)와 역광 셰이더(`pow(1.0-v)`)를 사용하였으나, 외곽선 페더링(Smoothstep) 및 터미네이터(음영 경계선) 수식 오류로 빛 샘 현상 발생.
    *   2차: Three.js의 `THREE.BackSide` (뒷면 렌더링을 통한 물리적 가림막) 및 `AdditiveBlending` 효과를 Cesium의 `CullFace.FRONT` 렌더 상태값으로 강제 포팅하여 완벽한 모사(Imitation)를 시도.
    *   최종 결정: 지구의 표면 텍스쳐나 3D 지형의 복잡성으로 인해 시각적 이질감이 발생함을 확인, **과감하게 커스텀 글로우 객체를 모두 롤백 및 파기**하여 순수한 Cesium 원본 해상도의 속도감을 유지하기로 선회 결정함.

## 📌 [19단계] 프론트엔드 테마(Themes) 렌더링 초고속 캐싱 아키텍처 전환 (완료 🎯 - 오늘 진행)
*   **비효율적 병렬 탐색 완전 폐기:** 앱 초기 구동 시 프론트엔드(`script_app.js`)에서 15개의 하드코딩된 테마를 검색하기 위해 15번의 무거운 Qdrant 백엔드 병렬 POST 쿼리를 난사하며 서버를 질식시키던 구조를 철거.
*   **O(1) 베이킹(Baking) 캐시 서빙 엔진 구축:** 백그라운드 워커가 새벽 등 유휴 시간에 200여 개의 광범위한 테마 데이터를 미리 구워서 저장(`themes_cache.json`)해 두면, 전용 라우터(`/api/themes`)가 단일 GET 통신으로 JSON 캐시를 즉각 읽어옵니다. 매 접속 시 9개의 테마를 무작위로 추려(random.sample) 단일 응답으로 고속 서빙하는 차세대 UI 최적화 아키텍처를 도입 완료.

## 📌 [20단계] 3D 지구 마커 클러스터링(Canvas 렌더링) 복구 및 고도화 (완료 🎯 - 오늘 진행)
*   일반적인 핀(Pin) 마커들이 대량으로 뭉쳐 UI를 방해하던 현상을 방지하기 위해 Cesium의 `GeoJsonDataSource.clustering` 기능을 부활시켰습니다.
*   동적으로 클러스터 아이콘이 생성될 때 끊김 현상을 막고 세련된 디자인을 얻고자, HTML Canvas API를 활용해 이중 링(블러 효과 + 메인 핵) 및 선명한 숫자가 새겨진 글라스모피즘 스타일의 컴포넌트를 설계하였습니다. 
*   또한 클러스터 규모(수량) 단위별로 동일한 Canvas B64 이미지를 한 번만 그려 메모리에 할당하는 캐싱(`clusterImageCache`) 아키텍처를 붙여 WebGL 렌더링 부하를 극소화했습니다.

## 📌 [21단계] 클러스터 3D 입체 돔(Sphere Dome) 렌더링 롤백 및 적용 (완료 🎯 - 오늘 진행)
*   **구체(Sphere) 누락 원인 파악:** CesiumJS의 내부 클러스터 처리 엔진(`EntityCluster`)은 극도의 렌더링 성능 최적화를 위해 오로지 `Billboard`, `Label`, `Point` 등 2D 요소 3가지만을 취급하며, 사용자가 강제로 `ellipse`(데칼)나 `ellipsoid`(3D 구체)를 주입하더라도 자체적으로 무시(Pass)하여 아예 렌더링을 하지 않는 치명적 한계를 발견했습니다.
*   **시각적 페이크(Fake) 3D 로직 도입:** 이 한계를 돌파하기 위해 다시 `Billboard` 엔진으로 회귀하되, HTML5 Canvas의 `createRadialGradient` (방사형 그라데이션) 기술을 극한으로 끌어올려 사용했습니다. 
*   **글라스모피즘 3D 구슬 생성:** 빛이 좌측 상단에서 내리쬐어 코어는 밝고 테두리로 갈수록 어두워지는 구면 질감을 음영으로 완벽히 시뮬레이션하고, `shadowOffsetY` 로 그라운드에 떠 있는 입체감을 부여했습니다. 렌더링 엔진은 이를 평면 이미지(Billboard)로 가볍게 처리하지만, 사용자의 눈에는 완벽한 3D 구슬 마커로 인식되는 최적화를 달성했습니다.

## 📌 [22단계] 클러스터 돔(Hemisphere) 절반 매립 트릭 구현 (완료 🎯 - 오늘 진행)
*   3D 구슬 홀로그램이 핀처럼 공중에 떠 있는 것이 아니라 땅에서 솟은 '반구(Dome)' 구조물처럼 보이게 해달라는 통찰력 있는 피드백을 적용.
*   빌보드의 기준점(`verticalOrigin`)을 **정중앙(CENTER)**으로 재수정하고 `CLAMP_TO_GROUND` 로 지표면에 박아넣음으로써, 3D 그래픽 엔진의 기본 뎁스(Depth) 차단 로직을 통해 "지평선 아래쪽 절반 캔버스 영역이 지구 표면에 의해 물리적으로 잘려 나가는(Clipping)" 완벽한 눈속임을 구현 완료.
*   구의 아랫도리가 흙에 묻히면서 글씨가 잘리지 않도록 텍스트 Y축 좌표(`fillText`)를 상단 방향으로 15% 정밀하게 끌어올렸습니다.

## 📌 [23단계] 클러스터 돔 롤백 및 솔리드 코어(Solid Core) 아우라 마커 디자인으로 수정 (완료 🎯 - 오늘 진행)
*   반구형(Dome) 모델링 시야에 대한 호불호 및 피드백을 수용하여, 즉각 기존 `BOTTOM` (세로 앵커) 기반의 부양 스탠딩 마커 구조로 원상 복구(Rollback)했습니다. 글씨 위치 또한 코어 정중앙으로 재정렬 완료.
*   단순한 단색 원형이 아니라 **가장자리 넓은 페더링(Feathering) 아우라** 기법을 도입하여 세련미를 살렸습니다. 
*   원의 중심부터 **20% 지점까지는 Alpha=1.0 (완전 불투명)** 상태를 유지하여 숫자 데이터를 확실히 가독할 수 있는 탄탄한 코어를 형성하고, **20% ~ 100% 지점(가장자리 끝자락) 구간은 Alpha=0.0 으로 서서히 넓게 소멸하는 방사형 그라데이션**을 적용하여 별이나 네온 코어처럼 은은하게 빛이 퍼져나가는 후광(Aura/Glow Dot) 느낌을 연출했습니다.

## 📌 [24단계] 위성 지도 라벨(글자) 텍스쳐 Esri 공식 레이어 마이그레이션 (완료 🎯 - 오늘 진행)
*   **텍스쳐 품질 한계 극복:** 기존에 사용하던 CartoDB Dark Matter 라벨 타일은 PNG 래스터 기반이라 확대 시 약간씩 깨지거나 아웃라인이 투박해지는 현상이 있었습니다.
*   현재 사용 중인 가장 고해상도의 압도적 위성 기저 맵(`Esri World Imagery`)과 완벽하게 1:1로 짝을 이루는 **`Esri Reference World Boundaries and Places`** 공식 라벨 타일로 엔진을 전격 교체했습니다.
*   이를 통해 훨씬 더 정교하고 선명한 국경선과 벡터 퀄리티의 깔끔하고 고급스러운 텍스트 아웃라인 템플릿을 장착 완료했습니다.

## 📌 [25단계] 지구본 초고해상도(Retina) 텍스쳐 강제 렌더링 파이프라인 개통 (완료 🎯 - 오늘 진행)
*   CesiumJS 모바일/데스크탑 환경에서 **흐릿하게 렌더링되거나(텍스쳐 로우 레볼루션 현상)** 타일 로딩 속도가 굼뜨는 문제를 완벽하게 뜯어고쳤습니다.
*   **하드웨어 원본 해상도(Native) 복원:** Cesium 엔진이 모바일 베터리와 그래픽 카드를 아끼겠다고 화면을 강제로 다운스케일링(1080p 이하) 해버리는 나쁜(?) 버릇인 `viewer.useBrowserRecommendedResolution = false;` 속성을 전면 해제해, 아이폰/MacBook의 4K 디스플레이 1:1 픽셀 매칭 화질을 이끌어냈습니다.
*   **SSE(최대 오차 허용치) 압송:** `maximumScreenSpaceError` 값을 기본 `2.0`에서 `1.5`로 낮추어, 카메라에 이미지가 도달하기 전에 훨씬 먼 거리에서도 고해상도 타일을 선제적으로 강제 스트리밍하도록 로딩 민감도를 한 차원 끌어올렸습니다.

## 📌 [26단계] 텍스트 라벨 Bloom 빛 번짐 방지 및 엔진 한계점(Material) 선별 (완료 🎯 - 오늘 진행)
*   **라벨(지명) 전용 Bloom 회피:** 지명 테스트에 글로우 효과가 먹혀 글씨가 눈부시게 퍼지는 현상을 잡기 위해 `textLayer.brightness` 를 기존 1.1에서 `0.95`로 소폭 다운그레이드 처리했습니다. 이로써 흰색 글꼴이 Bloom 필터의 임계점(Threshold) 아래로 내려가 빛 번짐의 타겟에서 벗어나도록 우회 회피(Bypass)를 달성했습니다.
*   **바다(Ocean) Roughness 한계점 점검:** 바다에만 반사광(Specular)이나 `roughness`를 먹이려면 엔진이 지도의 어느 픽셀이 땅이고 어느 픽셀이 물인지 인식해야 합니다. 이를 '워터 마스크(Water Mask)'라고 부르며 유료 3D 지형 데이터(Cesium World Terrain)에만 포함됩니다. 현재 사용하는 무료 Esri 위성 지도는 평면 그림(RGB)이기 때문에 아쉽게도 바다만 골라내어 재질감(Material)을 부여할 수는 없는 상태임을 기술적으로 최종 확인 및 확정지었습니다.

## 📌 [27단계] 사진 마커(Pin) 전면 폐기 및 디자인 완전 통합 (완료 🎯 - 오늘 진행)
*   Cesium 네이티브가 그리는 **디폴트 빨간색 카메라 핀(Pin)**을 데이터 레이어에서 영구적으로 폐기 및 소각 처리했습니다.
*   **클러스터 엔진의 맹점 보완:** 기존의 클러스터 엔진은 "마커가 3개 이상 모였을 때만 구슬 모양으로 합친다"는 규칙 때문에, 혼자 멀리 떨어진 1장짜리 사진이거나 줌인(Zoom-in) 시 거리가 벌어지면 엔진이 임의로 촌스러운 디폴트 핀으로 렌더링을 롤백하는 현상이 있었습니다.
*   **Aura Orb 팩토리 재설계:** 구슬(Aura Orb) 이미지를 생성하는 캔버스 로직을 모듈형 함수 `createAuraOrb(count)` 로 리팩토링했습니다. 이제 클러스터로 묶이지 않은 '방치된 낱개 사진(count=1)'까지 엔진을 강제로 루프(Loop)로 돌며 원형 구슬 이미지로 덮어씌우도록 설계했습니다. 이제 화면을 제아무리 확대하거나 축소해도 시야에서 빨간 핀은 100% 소멸했습니다.

## 📌 [28단계] 화면 가장자리 타일 비동기 프리로딩 및 단일 마커 시각 동기화 (완료 🎯 - 오늘 진행)
*   **시계(Frustom) 밖의 비동기 프리로딩:** 휴대폰이나 웹 화면의 렌더링 중심부가 지나치게 좁게 잡혀 화면 가장자리가 까맣게 지연 렌더링되던 한계를 수정했습니다. `viewer.scene.globe.preloadSiblings = true;` 속성을 투입하여, 카메라가 보고 있는 프레임을 넘어 주변부 타일들까지 엔진이 여유롭게 선제적으로 캐시(Cache)로 당겨오도록 스트리밍 알고리즘을 확장했습니다. 이제 화면을 스와이프할 때 검은 틈이 줄어듭니다.
*   **단일 사진용 숫자('1') 표기 제거:** 혼자 있는 마커 안에도 클러스터처럼 '1'이 적혀 나오는 것을 생략하도록 `if (count > 1)` 필터링을 걸었습니다.
*   **멀티플라이 셰이더(Color Tint) 충돌 버그 해결:** 기존 시스템에서 빨간 핀을 그리던 기본 설정상, 엔티티 속성에 `markerColor(빨간색 렌즈 틴트)`가 구워져 있었습니다. 그래서 같은 핑크빛 캔버스를 씌워도 단일 마커만 유독 피멍(?)이 든 것처럼 어둡고 탁하게 렌더링되고 있었는데, 이 속성을 `Cesium.Color.WHITE` 로 순정화 시키는 백지 코드를 주입하여 클러스터 구슬과 100% 동일한 톤의 아름다운 아우라 발광을 일치시켰습니다.

## 📌 [29단계] 카메라 외곽(안보이는 영역) 클러스터링 누락 버그 해결 (완료 🎯 - 오늘 진행)
*   **원인 규명:** 휴대폰 등 화면 밖(가장자리)을 스와이프할 때 간헐적으로 클러스터 연산이 되지 않는 시각적 버그(Popping)의 원인을 발견했습니다. 제가 배터리 절약을 위해 걸어두었던 `requestRenderMode: true` (화면을 멈추면 렌더링도 멈춤) 설정 때문에, 엔진이 화면 밖이나 카메라 끄트머리 영역에 대한 '클러스터 거리(Pixel Range) 연산'을 지연시킨 채 게으르게 화면을 갱신하던 문제였습니다.
*   **60fps 상시 렌더링 복귀:** 해당 배터리 절약 모드를 해제(`false`)하여 60fps 상시 연산 체제로 복귀했습니다. 이제 엔진은 1초에 60번씩 카메라 주변과 외곽 파티클 좌표의 거리를 쉬지 않고 스캐닝하여 묶음(Cluster) 처리를 미리 끝내놓기 때문에, 유저가 미친 듯이 스와이프를 해도 시각적 Popping(마커가 갑자기 구슬로 뭉치는 현상)이 발생하지 않습니다.

## 📌 [30단계] 클러스터 클릭 인터랙션 & 모달 갤러리 렌더러 도입 (완료 🎯 - 오늘 진행)
*   **블루(Blue) & 그린(Green) 선택 테마:** 맵에 떠 있는 기본 마커 색상을 요청하신 파란색(Blue)으로 전면 교체했습니다. 마커를 터치(클릭)하면 해당 마커가 선택되었다는 시각적 피드백으로 초록색(Green)으로 부드럽게 변환됩니다. (다른 마커를 누르거나 창을 닫으면 자동으로 원래 파란색으로 원상 복구됩니다).
*   **시간 역순(최신순) 정렬 보강:** 백엔드 Python 파이프라인(`api/routers/search.py`)을 개조하여, GeoJSON으로 좌표를 넘길 때 단순히 URL뿐만 아니라 EXIF 기반의 `date` 필드까지 통째로 패키징해 프론트엔드로 전달하도록 파이프라인 흐름을 뚫었습니다. 이를 통해 프론트엔드에서 날짜 역순(Descending) 정렬 연산이 완벽히 동작합니다.
*   **Glassmorphism UI 모달 주입:** Cesium DOM 위에 독립적인 모달 창을 띄우는 이벤트를 설계했습니다. 클러스터를 클릭 시, 해당 지역에서 찍힌 모든 사진들을 3열 썸네일 구조로 아름답게 정렬하여 띄워주며, 썸네일 터치 시 원본 사진을 확인할 수 있습니다.
*   **클러스터 Primitive Selection 버그 격파:** 초기 개발 시 클러스터를 클릭해도 이벤트가 무시(Silent drop)되는 치명적인 버그가 있었습니다. 원인을 분석한 결과, Cesium의 `EntityCluster` 모듈이 화면에 그려낸 3D 구슬 프리미티브(Primitive)를 클릭했을 때 반환하는 객체 고유 ID(`picked.id`)가 일반적인 단일 마커처럼 'Entity 객체'가 아니라, **'그 안에 뭉쳐진 원본 사진들의 Array(배열)' 자체를 반환**한다는 코어 엔진의 특이한 구조를 발견했습니다. 이에 맞춰 클릭된 대상이 Array인지 단일 Entity인지 추적 분기(Branch)를 완벽히 나누어 색상 변경 이벤트와 데이터 추출 렌더링이 문제없이 작동하도록 수정을 완료했습니다.

## 📌 [31단계] 3D 지구 클러스터 가장자리 파편화 버그 정복 및 렌더링 한계 돌파 (완료 🎯 - 오늘 진행)
*   **원인 규명 (Perspective Distortion Culling):** 줌아웃(축소) 시 정중앙의 구슬들은 잘 뭉치지만, 지구본 가장자리(지평선 너머)로 꺾여 넘어가는 구슬들이 뭉치지 않고 수천 개의 낱개로 와다다닥 부서져 쏟아지는 치명적 렌더링 버그가 발생했습니다. 이는 Cesium의 2D 픽셀 거리 연산(`ds.clustering`)이 지평선 곡률 뒤로 넘어가는 객체의 원근(Depth)을 왜곡된 공간으로 인지하여 클러스터링을 강제 포기(Culling)하기 때문에 발생하는 수학적 모순이었습니다.
*   **백엔드 지리 공간(Geo-Spatial) 격자화 시도 (v41~v44):** 픽셀 연산의 모순을 박멸하고자, 파이썬 서버에서 고도(Altitude)에 따라 위경도를 `math.floor` 연산으로 3,330km ~ 10km 단위의 바구니(Bucket)로 썰어서 던져주는 아키텍처를 시도했습니다. 하지만 픽셀(화면)에 맞춰 부드럽게 뭉치는 프론트엔드의 유동성을 따라가지 못하고, 특정 고도에서 구슬들이 겹치거나 선형적이지 않게 뭉치는 디자인적 한계에 부딪혔습니다.
*   **프론트엔드 깊이 테스팅 영구 무효화 (최종 해결 로직):** 백엔드 로직을 폐기(Rollback)하고 프론트엔드 엔진의 순수함을 유지한 채 타파했습니다.
    1.  **지형 높이 비동기 체이닝 해제:** `heightReference`를 `CLAMP_TO_GROUND` 에서 `NONE`으로 강제 변경해, 줌아웃 시 마커들이 산/빌딩 높이를 계산(Raycasting)하느라 클러스터 편입에 지각하는 병목 현상을 소각했습니다.
    2.  **무한대 심도(Depth) 투과 투영:** 모든 구슬과 핀 형상에 `disableDepthTestDistance = Number.POSITIVE_INFINITY` 를 각인. 지평선 뒤로 구겨지는 핀들조차 지형(Globe)의 투명도에 가려지지 않고 엔진이 무조건 최상단에 존재하는 것으로 인지하게 만들어, 시야각 가장자리 요소들도 100% 2D 픽셀 클러스터망 수식에 강제 합류시키는 기적을 이룩했습니다.
*   **초대형 숫자(Scale-up) 확장 포맷팅:** 9.9k 한도에 막혀 있던 클러스터 텍스트를 소수점 `k` 단위(e.g., `15.4k`) 및 최대 110px에 이르는 5단계 다이나믹 사이즈 구슬 캔버스로 리뉴얼하여, 수십만 장이 대륙 단위로 하나로 뭉쳐도 아름답게 담길 수 있게 UI 볼륨을 조율 완료했습니다.

## 📌 [32단계] 프리미엄 테마 갤러리 하이브리드 UX 및 지능형 지역 큐레이션 (완료 🎯 - 오늘 진행)
*   **비대칭 매거진(Masonry) 타일 레이아웃 연출:** 전통적인 좌우 슬라이딩의 단조로움을 탈피하여, `[슬라이드 2개] -> [3열 타일 그리드] -> [슬라이드 2개]`의 하이브리드 리듬감을 UI에 주입했습니다. 특히 4가지의 랜덤 CSS Grid 모자이크 패턴을 동적으로 부여해, 테마마다 사진 배치 크기가 테트리스처럼 제각각 비대칭을 이루는 프리미엄 매거진 스타일 뷰를 완성했습니다.
*   **고도화된 50/50 지역 테마 강제 배정 로직:** 방대한 160개의 캐시 풀 안에서 무작위로 9개의 테마를 뽑아 메인 화면에 뿌릴 때, 무조건 **50% 이상(5개)의 슬롯을 한국/글로벌 특정 지역(Location) 테마**로 강제 할당하도록 백엔드(`search.py`) 셔플 알고리즘을 개조했습니다.
*   **Qdrant 기반 지능형 장소 카운팅 (Random Tag 폐기):** 과거 `available_tags.json`에서 무지성으로 텍스트를 뽑아 사진 여부를 운에 맡기던 비효율적 40% 탈락 로직을 전면 소각했습니다. 
*   **10장 이상 실존 앨범 보장:** 이제 테마 복구 스레드가 Qdrant를 0.1초 만에 풀스캔(Scroll)하여 장소별 사진 개수를 집계(Counter)한 뒤, **"정확히 사진이 10장 이상 쌓여있는 튼튼한 지역"**만 색출해냅니다. 이 중 최대 70곳을 즉시 다이내믹 테마로 승격시켜 총 160개 규모의 거대하고 실패율 0%인 무결점 테마 캐시 팩을 실시간으로 구워내는(Baking) 시스템 진화를 달성했습니다.
*   **'Unknown Date' 최하단 렌더링 동기화:** 년도/날짜 정보가 없는 낡은 사진들이 테마의 썸네일 첫 화면을 차지하는 UX 결함을 타파하기 위해, 프론트엔드/백엔드 정렬 파이프라인에서 날짜 미상 데이터를 무조건 배열의 맨 끝(최하단)으로 유배시키는 렌더링 룰셋을 완벽히 적용했습니다.

## 📌 [33단계] 3D 지구 마커 렌더링 최적화, UI 레이아웃 안정화 및 캐시 로딩 한계점 등반 (완료 🎯 - 오늘 진행)
*   **프론트엔드 UI/UX 수치 동일화:** 메인 갤러리 섹션과 하단 테마 섹션 간 여백 불일치를 해결하기 위해, JavaScript 동적 개입을 전면 제거하고 전역 단일 CSS `.theme-section { margin-bottom: 25px; }` 로 철저히 하드코딩해 모든 화면 비율에서 완벽한 칼각 간격을 렌더링하도록 일원화했습니다.
*   **3D 지구 테마 무적 붉은색(Vibrant Red) 아이덴티티 적용:** 파란색 계열에서 Guma 브랜드 컬러에 맞춰 붉은 핏빛 네온(Red, `#f43f5e`) 틴트와 캔버스 코어 컬러 배합을 아우라 구슬에 적용하여, 검은색 우주 배경과 극명히 대비시키고 시인성을 폭발적으로 극대화했습니다.
*   **지평선/오지 마커 깜빡임(Jitter) 버그 모니터링:** '괌(Guam)'이나 '제주도' 등 바다 한가운데 외딴 오지의 소규모 마커들이 카메라의 기울기(Tilt) 변화 시 갑작스럽게 시야에서 사라지는 현상에 대한 원인 규명 진행. Cesium의 자체적인 스크린 지평선 클러스터 제거(Culling) 알고리즘 혹은 높이 스냅 로직 붕괴가 의심되어 여러 방어 코드를 시도/롤백하였으며 현재 모니터링 상태에 두었습니다.
*   **지구본 O(1) 정적 캐시(Static Cache) 최상위 설계 및 과감한 롤백/폐기:**
    *   **병목 규명:** 사용자가 `Map` 탭 이동 시 수만 개의 위치 데이터가 실시간으로 Qdrant DB에서 파싱되어 메인 스레드 프리징을 일으키는 아키텍처 결함 발견.
    *   **설계 시도:** 로직을 `map_service`로 분리/모듈화하고 새벽 3시에 5.9MB 분량의 `earth_cache.json`을 단독으로 구워낸 뒤, FastAPI 백엔드에서 `GZipMiddleware` 압축을 먹임으로써 600KB로 가볍게 압축해 CesiumJS의 비동기 Worker에 직접 던지는 차세대 로드 밸런싱을 구축.
    *   **파쇄 (Rollback):** 브라우저와 CesiumJS 코어 간의 충돌로 인해 지구가 먹통이 되는(Blank Earth) 치명적 크래시가 발생. UI 구동 안정성이 무엇보다 최우선이기에, 총 3개의 기획 커밋을 순식간에 **완전 무효화(`git reset --hard`)** 및 파기하여 원래의 락솔리드(Rock-solid)한 초기 스캔 버전으로 100% 원복 후 재배포 완료 처리했습니다.

## 📌 [34단계] 피드백 시스템 안전망 강화 및 글로벌 장소 자동완성 개편 (완료 🎯 - 오늘 진행)
*   **3D 지도 정밀 매핑을 위한 입력 제약(Constraint) 가동:** 피드백 모달에서 사용자가 임의의 텍스트를 손으로 쳐서 장소 오타나 쌩뚱맞은 지명을 넣을 경우 3D 지구상에서 좌표(위경도) 투영이 불완전해지는 오류를 원천 차단했습니다. 오직 하단 드롭다운 리스트(Kakao/OSM)의 검색 결과를 **'명시적으로 클릭'** 했을 때만 내부 페이로드 매핑 데이터가 발동하며, 미클릭 시 강제로 반려 경고창(lert)을 띄워 데이터 무결성을 보장합니다.
*   **하이브리드 글로벌 자동완성(Kakao + OSM 융합) 라우팅:** 국내 상세 스팟은 카카오(Kakao)가 압도적이지만 해외 지명은 잡아내지 못하는 점, 그리고 OSM은 글로벌 도시에 강력한 점을 동시에 취하기 위해 /api/location/search_global API를 전격 이식. 검색 시 **파란색 글로벌/해외 주소(OSM)** 영역과 **노란색 국내 상세 스팟(Kakao)** 구역을 드롭다운에 동시에 렌더링하는 UI 매퍼 레이아웃을 도입했습니다.
*   **Bounding Box 속성 매핑 정규화:** item.face_bbox_target으로 어긋나 있던 프론트엔드 크롭 제어 변수를 item.face_bbox로 정확히 수정하여, 갤러리 피드백 화면에서 안면 디텍팅 사각형 아웃라인 오작동을 해결.
*   **Celery 피드백 태스크(Task) 완전 폐기 및 단일화:** pi/tasks.py에 불필요하게 쪼개져 있던 개별 도메인 피드백 함수(eedback_time_loc, eedback_face)를 전면 삭제. 이제 피드백 관련 사이드 이펙트는 오직 dispatch_event (이벤트 허브) 를 통해 Pub/Sub 방식으로만 전파되도록 비동기 마이크로서비스 결합도를 극한으로 낮추어 코드 클린업을 이룩했습니다.
